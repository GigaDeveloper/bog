import { FilterByIdNumberPipe } from './filter-by-id-number.pipe';

describe('FilterByIdNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByIdNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
